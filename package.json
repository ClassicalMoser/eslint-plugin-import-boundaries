{
  "name": "eslint-plugin-import-boundaries",
  "type": "module",
  "version": "0.7.0",
  "description": "Enforce architectural boundaries with deterministic import paths",
  "author": "ClassicalMoser",
  "license": "ISC",
  "homepage": "https://github.com/ClassicalMoser/eslint-plugin-import-boundaries#readme",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/ClassicalMoser/eslint-plugin-import-boundaries.git"
  },
  "bugs": {
    "url": "https://github.com/ClassicalMoser/eslint-plugin-import-boundaries/issues"
  },
  "keywords": [
    "eslint",
    "eslint-plugin",
    "eslint-rule",
    "import",
    "boundaries",
    "architecture",
    "clean-architecture",
    "hexagonal-architecture",
    "directory-interface",
    "deterministic",
    "import-paths"
  ],
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "CHANGELOG.md",
    "LICENSE",
    "README.md",
    "dist/index.d.ts",
    "dist/index.js"
  ],
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "validate:fix": "pnpm run build && pnpm run lint:fix && pnpm run format && pnpm run typecheck",
    "validate:check": "pnpm run build && pnpm run lint && pnpm run format:check && pnpm run typecheck",
    "test": "vitest run",
    "test:watch": "vitest watch",
    "test:coverage": "vitest run --coverage",
    "test:mutation": "stryker run",
    "install:ci": "pnpm install --frozen-lockfile",
    "build": "tsdown --config tsdown.config.ts",
    "prepublishOnly": "npm run build && npm run test && npm run validate:check"
  },
  "peerDependencies": {
    "eslint": ">=9.0.0"
  },
  "devDependencies": {
    "@antfu/eslint-config": "^7.7.3",
    "@stryker-mutator/core": "^9.6.0",
    "@stryker-mutator/typescript-checker": "^9.6.0",
    "@stryker-mutator/vitest-runner": "^9.6.0",
    "@types/node": "^25.5.0",
    "@vitest/coverage-v8": "^4.1.2",
    "eslint": "^10.1.0",
    "eslint-config-prettier": "^10.1.8",
    "eslint-plugin-import-boundaries": "link:",
    "prettier": "^3.8.1",
    "tsdown": "^0.21.7",
    "typescript": "^6.0.2",
    "vitest": "4.1.2"
  }
}
