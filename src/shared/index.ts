/**
 * Shared: Types and utilities used by all layers.
 * No dependencies - just type definitions.
 */

export { DEFAULTS } from './types';
export type {
  AliasSubpathCheck,
  Boundary,
  BoundaryConfig,
  FileData,
  RuleOptions,
} from './types';
